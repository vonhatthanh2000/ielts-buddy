# Keep in sync with [project] dependencies in pyproject.toml (Vercel installs from pyproject).
# Lean set for serverless — no Whisper, no yt-dlp.
fastapi>=0.100.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0
pydantic[email]>=2.0.0
email-validator>=2.0.0
openai>=1.0.0
phidata>=2.0.0
python-dotenv>=1.0.0
supabase>=2.0.0
PyJWT>=2.8.0
bcrypt>=4.1.0
